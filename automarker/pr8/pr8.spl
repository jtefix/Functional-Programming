Int[] x[];
Int[] y[];

x[] = ReadStream;
y[] = ReadStream;

Int n;
n = 1;

Int s;
s = 0;

while(n <= sizeOf(x[])){
    output(x[n]);
    output(y[n]);

    s = s + x[n] - y[n];

    if(n%5==0){
        output(s);
        s = 0;
    }
    n++;
}