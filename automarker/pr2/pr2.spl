Int[] z[];
Int[] y[];
Int[] z[];

x[] = ReadStream;
y[] = ReadStream;
z[] = ReadStream;

Int n;
n = 1;

while(n <= sizeOf(x[])) {
    output(z[n]);
    output(y[n]);
    output(x[n]);
    output(x[n] + y[n]);
    output(y[n] + z[n]);
    n++;
}